<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <meta name="description" content="{{page.descriptopm}}"/>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    {{> styles}}
  </head>
  <body class="{{page.slug}}">
    {{> body}}
  </body>
  {{> scripts}}
</html>
